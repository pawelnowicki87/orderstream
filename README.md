# OrderStream

Food-ordering demo system: Spring Boot microservices (REST + gRPC + Kafka + WebSocket) with an Angular frontend and live order tracking.

Full documentation is written at the end of the build.
